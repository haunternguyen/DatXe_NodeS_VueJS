Server v0.0.1
===
- Final Project

### Quick start
- Install nodejs (>= v6.9), git.
- Install packages.
- copy file `.env.sample` to `.env` and config.

```shell
$ npm install 
```

- Run server (default port: `5858`)
```shell
$ npm start 
```
